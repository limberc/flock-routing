local_model:
  base_url: "http://localhost:11434/v1"
  model: "qwen2.5:9b"
  api_key: "none"

remote_model:
  base_url: "https://api.openai.com/v1"
  model: "gpt-4o"
  api_key: "${OPENAI_API_KEY}"

routing:
  complexity_threshold: 0.5   # 0.0–1.0; above this uses remote model

server:
  host: "0.0.0.0"
  port: 8080
